# 连续场景 · 真实 SRT 机制演示

从 383 期事故链的 57.733–79.266 秒片段提炼；保留原音频时间，仅作表现方法示例，不能作为独立核实的技术结论。

- `chapter.tsx`：同一主体跨六个 step，按 SRT cue 采样链接、选择框、闸杆和结果；无章节独立时钟。
- `timing.ts`：真实 gen 输出，章内秒与原 cue ID；实际项目需从自己的 SRT 重新生成。
- `chapter.css`：大对象、固定 px、安全区与主题 token。需要主题定义 `--danger`，没有时先由主线程补语义色。

复制到真实章节目录时同步 import、组件名、CSS 前缀、注册与 narrations。看方法，不复用这段事实/时间。完整浏览器与原声对照证据保存在本次 evaluation 目录，日常使用无需加载。
